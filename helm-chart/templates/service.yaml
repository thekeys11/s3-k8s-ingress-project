apiVersion: v1
kind: Service
metadata:
  name: {{ include "s3-nginx-ingress.fullname" . }}
  labels:
    {{- include "s3-nginx-ingress.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "s3-nginx-ingress.selectorLabels" . | nindent 4 }}
