apiVersion: v1
kind: ServiceAccount
metadata:
  name: s3-proxy-sa
  namespace: s3-proxy
  annotations:
    # Replace with your IAM role ARN
    # This enables IRSA - no API keys needed!
    eks.amazonaws.com/role-arn: arn:aws:iam::<ACCOUNT_ID>:role/s3-proxy-role
