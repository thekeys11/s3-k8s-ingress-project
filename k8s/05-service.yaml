apiVersion: v1
kind: Service
metadata:
  name: s3-proxy-service
  namespace: s3-proxy
  labels:
    app: s3-proxy
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: s3-proxy
