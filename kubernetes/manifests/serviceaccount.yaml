apiVersion: v1
kind: ServiceAccount
metadata:
  name: s3-reader-sa
  namespace: default
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::${AWS_ACCOUNT_ID}:role/s3-reader-role
    # This annotation binds the ServiceAccount to the IAM role via IRSA
automountServiceAccountToken: true
